import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { ArrowLeft, CalendarDays, Check, CheckCircle2, Circle, MessageSquare, Plus, Save, Trash2, UserPlus, X } from "lucide-react";
import { useEffect, useState, type FormEvent } from "react";
import { Link, useParams } from "react-router-dom";
import { MarkdownText } from "../components/MarkdownText";
import { Avatar, Badge, Button, EmptyState, Input, Spinner } from "../components/ui";
import { listProjectMembers } from "../services/projects";
import { addAssignee, addChecklistItem, createComment, deleteComment, getTask, listComments, removeAssignee, updateChecklistItem, updateComment, updateTask } from "../services/tasks";
import { useAuthStore } from "../stores/authStore";
import type { TaskPriority, TaskStatus } from "../types/domain";

export function TaskPage() {
  const { taskId } = useParams();
  const queryClient = useQueryClient();
  const user = useAuthStore((state) => state.user);
  const taskQuery = useQuery({ queryKey: ["task", taskId], queryFn: () => getTask(taskId!), enabled: Boolean(taskId) });
  const task = taskQuery.data;
  const members = useQuery({ queryKey: ["members", task?.project_id], queryFn: () => listProjectMembers(task!.project_id), enabled: Boolean(task) });
  const comments = useQuery({ queryKey: ["comments", taskId], queryFn: () => listComments(task!), enabled: Boolean(task) });
  const [description, setDescription] = useState("");
  const [progressDraft, setProgressDraft] = useState(0);
  const [comment, setComment] = useState("");
  const [newChecklist, setNewChecklist] = useState("");
  const [editComment, setEditComment] = useState<{ id: string; value: string } | null>(null);
  useEffect(() => { if (task) { setDescription(task.description ?? ""); setProgressDraft(task.progress); } }, [task]);
  const refresh = async () => { await Promise.all([queryClient.invalidateQueries({ queryKey: ["task", taskId] }), task && queryClient.invalidateQueries({ queryKey: ["tasks", task.project_id] })]); };
  const mutation = useMutation({ mutationFn: async (updates: Parameters<typeof updateTask>[1]) => { if (!task) return; await updateTask(task, updates); }, onSuccess: refresh });
  if (taskQuery.isLoading) return <div className="page-wrap flex min-h-72 items-center justify-center"><Spinner /></div>;
  if (taskQuery.error || !task) return <div className="page-wrap"><EmptyState icon={<Trash2 />} title="작업에 접근할 수 없습니다" description="프로젝트 멤버가 아니거나 삭제된 작업입니다." /></div>;
  const assignedIds = new Set(task.task_assignees?.map((item) => item.user_id));
  const checklist = task.task_checklist_items ?? [];
  const checklistProgress = checklist.length ? Math.round((checklist.filter((item) => item.completed).length / checklist.length) * 100) : 0;
  const saveDescription = (event: FormEvent) => { event.preventDefault(); mutation.mutate({ description }); };
  const toggleChecklist = async (itemId: string, completed: boolean) => { const item = checklist.find((entry) => entry.id === itemId); if (!item) return; await updateChecklistItem(item, completed); const nextCompleted = checklist.filter((entry) => entry.id === itemId ? completed : entry.completed).length; await updateTask(task, { progress_mode: "checklist", progress: checklist.length ? Math.round((nextCompleted / checklist.length) * 100) : 0 }); await refresh(); };
  const addChecklist = async (event: FormEvent) => { event.preventDefault(); if (!newChecklist.trim()) return; await addChecklistItem(task, newChecklist.trim(), checklist.length); setNewChecklist(""); await refresh(); };
  const postComment = async (event: FormEvent) => { event.preventDefault(); if (!comment.trim()) return; await createComment(task, comment.trim()); setComment(""); await queryClient.invalidateQueries({ queryKey: ["comments", taskId] }); };
  return <div className="page-wrap max-w-6xl"><Link to={`/projects/${task.project_id}/board`} className="mb-5 inline-flex items-center gap-2 text-xs font-semibold text-muted hover:text-brand"><ArrowLeft size={15} /> Board로 돌아가기</Link><div className="grid gap-5 xl:grid-cols-[1.5fr_.7fr]"><div className="space-y-5"><section className="panel p-5 sm:p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="mb-2 flex gap-2"><Badge tone={task.status === "done" ? "green" : task.status === "in_progress" ? "blue" : task.status === "review" ? "purple" : "neutral"}>{task.status}</Badge><Badge tone={task.priority === "urgent" ? "red" : task.priority === "high" ? "amber" : "neutral"}>{task.priority}</Badge></div><h1 className="text-2xl font-extrabold tracking-tight text-ink">{task.title}</h1></div><span className="text-xs text-muted">revision {task.revision}</span></div><form onSubmit={saveDescription} className="mt-6"><label className="label" htmlFor="task-description-edit">설명 · Markdown</label><textarea id="task-description-edit" className="field min-h-36" value={description} onChange={(event) => setDescription(event.target.value)} /><div className="mt-3 flex justify-end"><Button type="submit" size="sm" disabled={mutation.isPending}><Save size={14} /> 설명 저장</Button></div></form>{description && <div className="mt-5 rounded-xl border border-line bg-raised p-4"><MarkdownText>{description}</MarkdownText></div>}</section><section className="panel p-5 sm:p-6"><div className="flex items-center justify-between"><h2 className="font-extrabold text-ink">Checklist</h2><span className="text-xs font-bold text-brand">{task.progress_mode === "checklist" ? checklistProgress : task.progress}%</span></div><div className="mt-4 space-y-2">{checklist.map((item) => <button key={item.id} onClick={() => void toggleChecklist(item.id, !item.completed)} className="flex w-full items-center gap-3 rounded-xl border border-line p-3 text-left hover:bg-raised">{item.completed ? <CheckCircle2 className="text-emerald-500" size={19} /> : <Circle className="text-muted" size={19} />}<span className={`text-sm ${item.completed ? "text-muted line-through" : "text-ink"}`}>{item.content}</span></button>)}</div><form onSubmit={addChecklist} className="mt-3 flex gap-2"><Input value={newChecklist} onChange={(event) => setNewChecklist(event.target.value)} placeholder="체크리스트 항목" /><Button type="submit" variant="secondary"><Plus size={15} /></Button></form></section><section className="panel p-5 sm:p-6"><div className="flex items-center gap-2"><MessageSquare size={18} className="text-brand" /><h2 className="font-extrabold text-ink">댓글</h2><Badge>{comments.data?.length ?? 0}</Badge></div><div className="mt-5 space-y-4">{comments.data?.map((item) => <div key={item.id} className="flex gap-3"><Avatar name={item.author?.name ?? "사용자"} url={item.author?.avatar_url} size="sm" /><div className="min-w-0 flex-1 rounded-xl border border-line bg-raised p-3"><div className="flex items-center justify-between gap-3"><div><span className="text-xs font-bold text-ink">{item.author?.name}</span><span className="ml-2 text-[10px] text-muted">{new Date(item.created_at).toLocaleString("ko-KR")}</span></div>{item.author_id === user?.id && <div className="flex"><Button size="sm" variant="ghost" onClick={() => setEditComment({ id: item.id, value: item.content ?? "" })}>수정</Button><Button size="sm" variant="ghost" className="text-red-600" onClick={async () => { await deleteComment(item.id); await queryClient.invalidateQueries({ queryKey: ["comments", taskId] }); }}><Trash2 size={13} /></Button></div>}</div>{editComment?.id === item.id ? <form className="mt-2 flex gap-2" onSubmit={async (event) => { event.preventDefault(); await updateComment(task, item, editComment.value); setEditComment(null); await queryClient.invalidateQueries({ queryKey: ["comments", taskId] }); }}><Input value={editComment.value} onChange={(event) => setEditComment({ ...editComment, value: event.target.value })} /><Button type="submit" size="sm"><Check size={14} /></Button><Button size="sm" variant="ghost" onClick={() => setEditComment(null)}><X size={14} /></Button></form> : <div className="mt-2"><MarkdownText>{item.content ?? ""}</MarkdownText></div>}</div></div>)}</div><form onSubmit={postComment} className="mt-5"><textarea className="field min-h-24" value={comment} onChange={(event) => setComment(event.target.value)} placeholder="댓글을 입력하세요. Markdown, code block, @학번 mention을 지원합니다." /><div className="mt-2 flex justify-end"><Button type="submit" disabled={!comment.trim()}>댓글 작성</Button></div></form></section></div><aside className="space-y-5"><section className="panel p-5"><h2 className="font-extrabold text-ink">작업 속성</h2><label className="label mt-5" htmlFor="task-status">상태</label><select id="task-status" className="field" value={task.status} onChange={(event) => mutation.mutate({ status: event.target.value as TaskStatus })}><option value="todo">TODO</option><option value="in_progress">진행 중</option><option value="review">검토</option><option value="done">완료</option></select><label className="label mt-4" htmlFor="task-priority-detail">우선순위</label><select id="task-priority-detail" className="field" value={task.priority} onChange={(event) => mutation.mutate({ priority: event.target.value as TaskPriority })}><option value="low">낮음</option><option value="medium">보통</option><option value="high">높음</option><option value="urgent">긴급</option></select><label className="label mt-4" htmlFor="task-progress">진행률 {progressDraft}%</label><input id="task-progress" type="range" min="0" max="100" value={progressDraft} onChange={(event) => setProgressDraft(Number(event.target.value))} onPointerUp={() => mutation.mutate({ progress_mode: "manual", progress: progressDraft })} onKeyUp={() => mutation.mutate({ progress_mode: "manual", progress: progressDraft })} className="w-full accent-blue-600" /><label className="label mt-4" htmlFor="task-due-detail">마감일</label><Input id="task-due-detail" type="date" value={task.due_date ?? ""} onChange={(event) => mutation.mutate({ due_date: event.target.value || null })} /></section><section className="panel p-5"><div className="flex items-center justify-between"><h2 className="font-extrabold text-ink">담당자</h2><UserPlus size={17} className="text-brand" /></div><div className="mt-4 space-y-2">{task.task_assignees?.map((assignee) => <div key={assignee.user_id} className="flex items-center gap-2 rounded-xl border border-line p-2"><Avatar name={assignee.profile?.name ?? "팀원"} url={assignee.profile?.avatar_url} size="sm" /><span className="min-w-0 flex-1 truncate text-xs font-semibold text-ink">{assignee.profile?.name}</span><Button variant="ghost" size="sm" className="h-7 w-7 p-0 text-red-600" onClick={async () => { await removeAssignee(task.id, assignee.user_id); await refresh(); }}><X size={13} /></Button></div>)}{!task.task_assignees?.length && <p className="text-xs text-muted">미배정</p>}</div><label className="label mt-4" htmlFor="add-assignee">+ 담당자 추가</label><select id="add-assignee" className="field" defaultValue="" onChange={async (event) => { if (!event.target.value) return; await addAssignee(task.id, event.target.value); event.target.value = ""; await refresh(); }}><option value="">프로젝트 멤버 선택</option>{members.data?.filter((member) => !assignedIds.has(member.user_id)).map((member) => <option key={member.user_id} value={member.user_id}>{member.profile?.name} ({member.profile?.student_id})</option>)}</select></section><section className="panel p-5"><div className="flex items-center gap-2 text-sm text-muted"><CalendarDays size={16} /> 생성 {new Date(task.created_at).toLocaleDateString("ko-KR")}</div></section></aside></div></div>;
}
